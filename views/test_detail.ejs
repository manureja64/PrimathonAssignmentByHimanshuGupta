<!DOCTYPE html>
<html lang="en">
<%- include('layouts/head');-%>
    <%- include('layouts/header');-%>

        <style>
            .update {
                display: flex;
                justify-content: space-evenly;
            }
        </style>

        <body>
            <!-- Navigation-->
            <%- include('layouts/navbar');-%>
                <div class="update">
                    <span>
                        <a href="<%= test.url+'/update' %>">Update Test</a>
                    </span>
                </div>
                <hr>

                <h1><u>Test</u>: <%- test.title %>
                </h1>
                <h2><u>Number of Options</u>: <%= test.numOfOptions %>
                </h2>
                <h3><u>Test Duration</u>: <%= test.duration %> minutes
                </h3>
                <h3>
                    <u>Class</u>: <% if (test.class===0) { %>
                        All Classes
                        <% } else if (test.class>0) { %>

                            <%= test.class %>th
                                <% } else { %>
                                    None (for this test no class is selected. Possibly, this test is uploaded BEFORE
                                    version
                                    4 {the features like 'subject' and 'class' were created for the test model})
                                    <% } %>
                </h3>

                <h3>
                    <u>Subject</u>: <% if (test.subject===0) { %>
                        All
                        <% } else if (test.subject===1) { %>
                            Geography

                            <% } else if (test.subject===2) { %>
                                History

                                <% } else if (test.subject===3) { %>
                                    Civics
                                    <% } else if (test.subject===4) { %>
                                        Science
                                        <% } else if (test.subject===5) { %>
                                            Economics
                                            <% } else { %>
                                                None
                                                <% } %>
                </h3>

                <h3><u>Chapter</u>: <% if (test.chapter>=0) { %>
                        <%= test.chapter %>
                            <% } else { %>
                                Test Chapter has not been defined for this test.
                                <% } %>
                </h3>
                <h3><u>Test Type</u>: <% if (test.type===0) { %>
                        Sectional Test
                        <% } else { %>
                            Full Length Test
                            <% } %>
                </h3>

                <h3><u>Test Message</u>: <%- test.message %>
                </h3>
                <h3><u>Test Notes</u>: <%- test.notes %>
                </h3>


                <h4>Directions</h4>
                <div style="margin-left: 20px;margin-top: 20px;">

                    <ol>
                        <% for( let i=0; i < test_directions.length; i++ ) { %>
                            <li>
                                <a href="<%= test_directions[i].url %>" target="_blank">
                                    <%- test_directions[i].direction %>
                                </a>
                            </li>

                            <% } %>
                    </ol>
                    <dl>
                        <% if (test_directions.length===0) { %>
                            <p>This Test has No Directions</p>

                            <% } %>
                                <hr>
                                <dt>
                                    <h4><a href="/catalog/direction/create/<%= test._id %> " target="_blank">Create a
                                            Direction</a>
                                    </h4>
                                </dt>
                                <hr>
                    </dl>
                </div>

                <h4>Questions</h4>
                <div style="margin-left: 20px;margin-top: 20px;">

                    <ul>
                        <% for( let i=0; i < test_questions.length; i++ ) { %>
                            <li>
                                <a href="<%= test_questions[i].url %>" target="_blank">
                                    Question <%= i+1 %>
                                </a>: <%- test_questions[i].question %>
                            </li>

                            <% } %>
                                <% if (test_questions.length===0) { %>
                                    <p>This Test has No Questions</p>

                                    <% } %>

                    </ul>
                </div>
                <hr>
                <div class="update">
                    <span>
                        <a href="<%= test.url+'/update' %>">Update Test</a>
                    </span>
                    <span>
                        <a href="<%= test.url+'/delete' %>">Delete Test</a>
                    </span>
                </div>
                <hr>

        </body>

        <script src="js/index.js"></script>

</html>